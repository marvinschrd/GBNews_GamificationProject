using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CanvasScript : MonoBehaviour
{

    [Header("Panels")] [SerializeField] private List<GameObject> CanvasPanels;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    // Initialize and enable/call the various panels
    void IniTializePanels()
    {
        
    }

    // Disable and make the panels disapear
    void HidePanels()
    {
        
    }
}
